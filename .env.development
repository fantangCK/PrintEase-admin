# 【开发】环境变量

# 访问模式：frontend（前端控制）或 backend（后端控制）
# PrintEase 后端未提供框架内置的 /api/v3/system/menus 接口
# 因此必须使用 frontend 模式，通过前端 asyncRoutes 配置菜单和权限
VITE_ACCESS_MODE = frontend

# 应用部署基础路径（如部署在子目录 /admin，则设置为 /admin/）
VITE_BASE_URL = /

# API 请求基础路径（开发环境设置为 / 使用代理，生产环境设置为完整后端地址）
VITE_API_URL = /

# 代理目标地址（开发环境通过 Vite 代理转发请求到此地址，解决跨域问题）
VITE_API_PROXY_URL = https://api.fantangck.top:3000

# Delete console
VITE_DROP_CONSOLE = false
